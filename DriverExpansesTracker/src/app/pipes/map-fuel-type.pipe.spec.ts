import { MapFuelTypePipe } from './map-fuel-type.pipe';

describe('MapFuelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MapFuelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
